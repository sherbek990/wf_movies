<div class="admin-container">
  <div class="admin-header">
    <h1>Admin Dashboard</h1>
    <div style="display: flex; gap: 1rem;">
      <a class="btn primary" href="/admin/content/new">✚ Add Content</a>
      <a class="btn secondary" href="/admin/genres">🏷 Manage Genres</a>
    </div>
  </div>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <% if (content && content.length > 0) { %>
    <table class="admin-table">
      <thead>
        <tr>
          <th>Poster</th>
          <th>Title</th>
          <th>Category</th>
          <th>Genre</th>
          <th>Year</th>
          <th>Rating</th>
          <th>Views</th>
          <th>Featured</th>
          <th>Banner</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% content.forEach(function(item) { %>
          <tr>
            <td>
              <img src="<%= item.image_url || 'https://via.placeholder.com/60x90/1a1a1a/666' %>" alt="<%= item.title %>" />
            </td>
            <td><strong><%= item.title %></strong></td>
            <td><%= item.category %></td>
            <td><%= item.genre_name || '-' %></td>
            <td><%= item.year || '-' %></td>
            <td><%= item.rating || '0.0' %></td>
            <td><%= item.views || 0 %></td>
            <td><%= item.is_featured ? '✅' : '❌' %></td>
            <td><%= item.is_banner ? '✅' : '❌' %></td>
            <td>
              <div class="admin-actions">
                <a class="btn small secondary" href="/watch/<%= item.id %>" target="_blank">👁 View</a>
                <a class="btn small" style="background: var(--secondary);" href="/admin/content/<%= item.id %>/episodes">📺 Episodes</a>
                <a class="btn small primary" href="/admin/content/<%= item.id %>/edit">✏ Edit</a>
                <form method="post" action="/admin/content/<%= item.id %>/delete" style="display:inline-block" onsubmit="return confirm('Are you sure you want to delete this content?');">
                  <button class="btn small danger" type="submit">🗑 Delete</button>
                </form>
              </div>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <div style="text-align: center; padding: 3rem; color: var(--text-secondary); background: var(--card-bg); border-radius: 10px;">
      <h3>No content available</h3>
      <p style="margin: 1rem 0;">Start by adding your first movie, series, or anime</p>
      <a class="btn primary" href="/admin/content/new">✚ Add Content</a>
    </div>
  <% } %>
</div>
